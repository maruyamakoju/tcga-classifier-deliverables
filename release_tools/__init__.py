"""Shared helpers for the release audit/validate/build scripts.

Dependency-free (stdlib only) so it can be shipped inside release-lite/
alongside the scripts that import it, the same way tcga_rnaseq/ is shipped
for the scoring CLIs.
"""
