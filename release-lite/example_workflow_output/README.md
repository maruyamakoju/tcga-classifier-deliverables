# Example workflow output

This directory shows the expected output shape from:

```bash
python run_tumor_normal_workflow.py example_input.csv --labels example_labels.csv --output-dir example_workflow_output --top-n 3
```

The files are intentionally small and are included as a visual/reference
example for users. Use `workflow_report.md` as the first file to inspect.
