[pytest]
testpaths = tests
markers =
    slow: full-data reproduction tests requiring the 14850-gene feature matrix (set TCGA_FEATURES)
addopts = -ra --strict-config --strict-markers -p no:asyncio
filterwarnings =
    error
